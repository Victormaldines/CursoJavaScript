describe('TESTING SOMETHING (IT)', () => {
  it('should return one', () => {
    const number = 1;
    expect(number).toBe(1);
  });
});

describe('TESTING SOMETHING (TEST)', () => {
  test('should return Luiz', () => {
    const name = 'Luiz';
    expect(name).toBe('Luiz');
  });
});
